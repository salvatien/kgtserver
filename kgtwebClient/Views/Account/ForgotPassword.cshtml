
@using Dogs.ViewModels.Data.Models.Account;
@model LoginModel

@{
    ViewBag.Title = "Zresetuj hasło";
}
<div style="margin:60px">
    <h2>@ViewBag.Title.</h2>
    <div class="row">
        <div class="col-md-8">
            <section id="resetPasswordForm">
                @using (Html.BeginForm("ForgotPassword", "Account", null, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                {
                    @Html.AntiForgeryToken()
                    <h4>Podaj login lub adres email konta, którego hasło ma zostać zresetowane. </h4>
                    <hr />
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div class="form-group">
                        <input type="text" placeholder="Login lub email" id="userNameOrEmailInput" name="userNameOrEmail">
                    </div>
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Wyślij" class="btn btn-default" />
                        </div>
                    </div>
                    
                }
            </section>
        </div>
        
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

